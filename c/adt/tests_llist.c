#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <assert.h>
#include "llist.h"

void quit(char *msg) {
    perror(msg);
    exit(EXIT_FAILURE);
}


int main(int argc, char* argv[]) {

    printf("-----------------\n");
    printf("All Tests Passed!\n");

    return 0;
}