##
##  Makefile -- Build procedure for sample sstats Apache module
##  Autogenerated via ``apxs -n sstats -g''.
##

builddir=.
top_srcdir=/usr/lib/httpd
top_builddir=/usr/lib/httpd
include /usr/lib/httpd/build/special.mk

#   the used tools
APACHECTL=apachectl

#   additional defines, includes and libraries
#DEFS=-Dmy_define=my_value
#INCLUDES=-I./
#LIBS=-Lmy/lib/dir -lmylib

#   the default target
all: local-shared-build

#   install the shared object file into Apache 
install: install-modules-yes

#   cleanup
clean:
	-rm -f mod_sstats.o mod_sstats.lo mod_sstats.slo mod_sstats.la 

#   simple test
test: reload
	lynx -mime_header http://localhost/sstats

#   install and activate shared object by reloading Apache to
#   force a reload of the shared object file
reload: install restart

#   the general Apache start/restart/stop
#   procedures
start:
	$(APACHECTL) start
restart:
	$(APACHECTL) restart
stop:
	$(APACHECTL) stop

